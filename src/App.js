import Button from "./Button"; // import 해주면 Button 을 아래에서 사용할 수 있는 상태가 된다.

function App() {
  return (
    <div>
      <h1>Welcome back!!!!</h1>
      <Button text={"Continue"} />
    </div>
  );
}

export default App;